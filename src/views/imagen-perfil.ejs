<main class="contenedor contenedor-formularios">
    <h1><%= nombrePagina %></h1>
    <form class="default-form" method="POST" enctype="multipart/form-data">
        <legend>Sube tu imagen que se mostrara junto a tu nombre y biografia</legend>
        <div class="campo">
            <label>Imagen:</label>
            <input type="file" name="imagen">
        </div>
        <% if (usuario.imagen) { %>
            <div class="campo">
                <label>Imagen Actual:</label>
                <img src="/uploads/perfiles/<%= usuario.imagen %>" width="400" height="400" alt="">
            </div>
        <% } else { %>
            <p>No hay imagen Actual</p>
        <% } %> 
        <div class="campo enviar">
            <input type="submit" value="Cambiar Imagen" class="btn btn-rosa">
        </div>
    </form>
</main>